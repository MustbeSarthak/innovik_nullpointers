[build]
  publish = "client/dist"
  command = "npm --prefix client install && npm --prefix client run build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200